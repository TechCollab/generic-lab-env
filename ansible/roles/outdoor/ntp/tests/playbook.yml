---
- name: Test play 
  hosts: all  
  become: true
  gather_facts: true
  vars:
    ntp_timezone: 'Europe/Sofia'
    ntp_pkg_state: 'latest'
    #ntp_service_state: 'stopped'
    ntp_service_enabled: 'no'
  roles:
    - ansible-role-ntp
